const page = (state = 'week', action) => {
  return state;
};

const isEditingTask = (state = false, action) {
  switch (action.type) {
    case value:

      break;

    default:
      break;
  }



  if (action.type === 'EDIT_TASK') {

  }
  }
  return action.isEditingTask
}
