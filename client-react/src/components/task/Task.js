import React, { Component } from 'react';
import { taskStyles } from './TaskStyle';

export class Task extends Component {
  render() {
    return (
      <div style={taskStyles.root}>
        t
      </div>
    );
  }
}
